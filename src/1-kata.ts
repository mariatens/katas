// Write a function that takes an array of words and smashes them together into a sentence and returns the sentence. 

function sentenceSmash(sentArr: string[]): string {
    return sentArr.join(' ')

}

export default sentenceSmash